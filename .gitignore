node_modules/
.next/
.open-next/
.wrangler/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Local database + uploaded media are content, not code.
# Back them up by copying the folders; never commit them.
data/
public/uploads/*
!public/uploads/.gitkeep

.env
.env.local
.env*.local
.DS_Store

# Local instructions for Codex/agents. Keep this on each developer's machine,
# never in the application repository.
/agents.md

.vscode/
